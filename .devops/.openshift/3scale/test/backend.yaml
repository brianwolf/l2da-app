apiVersion: capabilities.3scale.net/v1beta1
kind: Backend
metadata:
  name: api-sso-test
  namespace: 3scale
spec:
  name: api-sso-test
  systemName: api-sso-test
  privateBaseURL: "https://jsonplaceholder.typicode.com/posts/1/comments"
  providerAccountRef:
    name: test-tenant-secret # esto apunta a un secret con la url y el token del 3scale donde lo queres desplegar
